// lib/theme/app_colors.dart
import 'package:flutter/material.dart';

/// --- Brand Palette (Purple) ---
class AppColors {
  // Primary Brand Color
  static const Color navyblue = Color(0xFF37474F);

  // Darker shade for buttons / shadows
  static const Color navyblueDark = Color(0xFF263238);

  // Light tint for backgrounds / accents
  static const Color navyblueLight = Color(0xFFCFD8DC);

  // Main text
  static const Color textDark = Color(0xFF1E1E1E);

  // Muted text
  static const Color textMuted = Color(0xFF6B7280);

  // Soft background (soft off-white with a hint of violet)
  static const Color bg = Color(0xFFF5F7F9);
}


